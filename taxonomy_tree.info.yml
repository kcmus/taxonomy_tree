name: Taxonomy Tree
description: A simple service that loads the tree of a vocabulary in a multidimensional array, similar to what the menu system does.
core: 8.x
type: module
dependencies:
  drupal:taxonomy